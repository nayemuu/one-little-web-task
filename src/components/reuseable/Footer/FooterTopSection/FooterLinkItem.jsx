import React from "react";

const FooterLinkItem = ({ children, item }) => {
  return (
    <div className="text-[14px] leading-[16px] font-medium">{children}</div>
  );
};

export default FooterLinkItem;
